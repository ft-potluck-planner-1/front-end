


const initialState = {
    username:'',
    password: '',
    email: '',
}

const Reducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }

}

export default Reducer;